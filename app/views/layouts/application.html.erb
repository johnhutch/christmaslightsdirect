<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Rails Devise" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Devise" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body class="app_grid">
    <header class="site_head">
      <div class="container">
        <a href="/" title="Christmas Lights Direct" class="logo">
          <h1 class="page_title">Christmas Lights Direct</h1>
          <img src="/images/logo.png" alt="Christmas Lights Direct logo" />
        </a>
        <h2 class="description">Direct importers of wholesale christmas lights and decorations</h2>
      </div>
    </header>
    <div class="site_subheader"> </div>
    <%= render 'layouts/navigation' %>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
    <footer>
        <%= render 'layouts/nav_links_for_auth' %>
    </footer>
  </body>
</html>
